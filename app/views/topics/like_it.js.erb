$('.topic .pull-right a i.icon-heart').toggleClass('like_red', <%= current_user.is_like @topic %>)
$('.topic .pull-right span.like_count').html(<%= @topic.likes.count %>)
//$('#like').html('<%#= escape_javascript(render :partial => "like") %>')

